# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# IDE
.idea/

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Coverage
coverage/

# Logs
*.log

# Debug / browser-test artifacts (Playwright MCP screenshots, page-*.yml,
# console logs) — ephemeral, never committed. Durable evidence for a task is
# the ticket attachment, not a repo file.
.debug/
.playwright-mcp/

# Build-agent worktrees (scripts/reap-build-leaks.mjs prunes stale ones).
.claude/worktrees/

ignored



# Claude Code: shared project config (agents, commands) IS repo content;
# only user-local state stays ignored.
.claude/settings.local.json
.claude/*.local.*
