/** MCP endpoint + API key errors. */
export const mcp = {
  errors: {
    missingKey: 'mcp.errors.missingKey',
    invalidKey: 'mcp.errors.invalidKey',
    keyNotFound: 'mcp.errors.keyNotFound',
  },
} as const;
